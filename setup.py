from setuptools import setup

setup(name='new_test_repo',
      version='0.1',
      description='contains many data science modules',
      author='Akash Kumar',
      packages=['hello_world'],
      zip_safe=False)